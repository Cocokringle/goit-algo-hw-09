# Порівняння ефективності алгоритмів розміну монет

## 🔎 Аналіз результатів

### 1. Жадібний алгоритм (`find_coins_greedy`)

**Часова складність:** $O(N)$  
Де $N$ — кількість номіналів монет (у нашому випадку 6: [50, 25, 10, 5, 2, 1]).

**Опис:** Цей алгоритм працює дуже швидко. Його складність залежить лише від кількості видів монет,а не від суми, яку потрібно розміняти. Навіть якщо сума буде 1 мільярд, алгоритм виконає лише кілька операцій ділення та віднімання для кожного номіналу.

**Переваги:**

- Миттєве виконання навіть для величезних сум.
- Використовує мінімум пам'яті ($O(1)$).

**Недоліки:**

- Не гарантує знаходження мінімальної кількості монет для всіх можливих наборів номіналів (хоча для стандартного набору монет він працює коректно).

---

### 2. Динамічне програмування (`find_min_coins`)

**Часова складність:** $O(A \cdot N)$  
Де $A$ — сума розміну, а $N$ — кількість номіналів монет.

**Опис:** Алгоритм будує рішення крок за кроком, обчислюючи оптимальну кількість монет для кожної суми від 1 до $A$. Це означає, що час виконання зростає лінійно пропорційно до суми.

**Продуктивність при великих сумах:** При збільшенні суми `amount` ефективність різко падає.

- Якщо `amount = 100`, цикл виконається приблизно 600 разів.
- Якщо `amount = 1,000,000`, цикл виконається 6 мільйонів разів.
  Це також вимагає значних витрат пам'яті для зберігання таблиці результатів для всіх проміжних сум.

**Переваги:**

- Гарантовано знаходить найоптимальніший варіант для будь-якого набору монет.

**Недоліки:**

- Дуже повільний для великих сум.
- Високе споживання пам'яті ($O(A)$) для збереження масивів.

---

## 🏆 Загальні висновки

1. **Швидкість:** Жадібний алгоритм перемагає з величезним відривом. На великих сумах (наприклад, > 10,000) час виконання динамічного алгоритму стає відчутним, тоді як жадібний залишається миттєвим.
2. **Оптимальність:** Для даного набору монет `[50, 25, 10, 5, 2, 1]` жадібний алгоритм дає той самий результат, що й динамічний, тому використання динамічного програмування тут є надлишковим.
3. У реальних касових системах зі стандартною валютою краще використовувати **жадібний алгоритм** через його швидкодію та малі вимоги до пам'яті. Динамічне програмування варто застосовувати лише для специфічних наборів монет, де жадібний підхід не працює, або коли суми розміну невеликі.
